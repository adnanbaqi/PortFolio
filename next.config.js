/** @type {import('next').NextConfig} */
const nextConfig = {
  // REMOVE or comment out static export mode
  // output: 'export',

  images: {
    unoptimized: true
  }
}

module.exports = nextConfig;
